# Racepoint AI

The public project hub for Racepoint AI. Hosted on Vercel and connected to `racepoint.ai`.

## Publishing future projects

Add a linked project row to `index.html`, or connect a new Vercel project to a subdomain such as `project.racepoint.ai`.
